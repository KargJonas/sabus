{
  "name": "counter-workers-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "example:reader-writer": "npm run build && node dist/examples/reader-writer/main.js",
    "example:arrays": "npm run build && node dist/examples/arrays/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.4.5"
  }
}
